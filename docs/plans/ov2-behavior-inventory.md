# ov2 Behavior Inventory

Every row: source location, behavior, classification, v2 resolution.
Classifications: CONTRACT (port exactly, test it) · BUG (do not port; v2 does
it right; note the fix) · DECIDE (deliberate divergence; record the decision).

Rows 1–10 are the seed rows from the phase 0 plan (line numbers verified
against the code). Rows 11+ continue the walk file by file. Row numbers are
stable — later additions append, never renumber.

| # | Source | Behavior | Class | v2 resolution |
|---|---|---|---|---|
| 1 | vault.sh:990-1003 | `mocs orphan` sets found_in_moc in a pipeline subshell; flag never propagates; every note reported orphaned | BUG | reimplement with real [[wikilink]] parsing (phase 1) |
| 2 | vault.sh:993 | orphan match is substring grep, not link-aware | BUG | same fix as #1 |
| 3 | vault.sh:156 vs triage_llm.py:200 | slugify budget 60 (capture) vs 80 (triage) | DECIDE | kept deliberately — python comment says filed notes get a larger budget; one Slugify(s, maxLen) (slugify_test.go:10) |
| 4 | triage_llm.py:342-363 | `moc: [[MOC Music]]` frontmatter parses as ["[MOC Music]"]; rename sync depends on it | CONTRACT | ported; TestWikilinkQuirk (frontmatter_test.go:54) |
| 5 | triage_llm.py:489-492 | non-null body_patch is APPLIED but never DISPLAYED in approval | BUG | v2 Validate rejects non-null body_patch / non-empty links_to_add (phase 3) |
| 6 | triage_llm.py:456 | vault containment via string prefix — accepts /path/vault-evil | BUG | EvalSymlinks + filepath.Rel (phase 2) |
| 7 | vault.sh:859,966 | obsidian://open?vault=main-vault hardcoded | BUG | vault name from config (phase 2) |
| 8 | vault.sh:470-482 | MOC entry placement prefers emoji headings chain | DECIDE | simplified: append under "## 🔗 Recent Additions", create if missing (spec §CLI/TUI) |
| 9 | vault.sh:372-406 | picker stdout carries ONLY the selection; prompts to stderr; fzf opens /dev/tty | CONTRACT | tui package tty discipline (phase 2) |
| 10 | vault.sh:581-587 | capture body from stdin when piped | CONTRACT | frozen AGENTS.md subset |
| 11 | vault.sh:22 + triage_llm.py:68-70 | `$OV_CONFIG` env var overrides the default config path in both readers | CONTRACT | ported (config_test.go:88 TestOVConfigEnvSelectsFile) |
| 12 | vault.sh:25 | config file is `source`d as bash — arbitrary code execution; command substitution works in values | DECIDE | v2 reads TOML, no execution; legacy bash configs migrated once by `ov2 init` via ParseLegacy |
| 13 | vault.sh:27-31 | OV_VAULT_DIR unset → error on stderr pointing at the config file, exit 1 | CONTRACT | ported; `ov2 doctor` diagnoses, vault-needing commands fail with the same pointer |
| 14 | vault.sh:41 | load_config runs at script start for EVERY invocation — even `ov help` dies without a config | BUG | v2 cobra: help/version work configless; config loaded per-command |
| 15 | vault.sh:33 vs triage_llm.py:78,584 | vault-dir expansion: bash full `eval` (injection-prone), python expandvars+expanduser — two different grammars | DECIDE | one rule: leading `~` and `$VAR`/`${VAR}` expand in VaultDir only (config_test.go:68 TestVaultDirExpansion) |
| 16 | vault.sh:34-39 + triage_llm.py:51-60 | PARA dir names configurable, defaults 00-Inbox / 01-Projects / 02-Areas / 03-Resources / 04-Archive / 99-Meta | CONTRACT | ported (config_test.go TestLoadDefaultsOnly) |
| 17 | vault.sh:52-103 | show_help: static usage text | no observable contract | v2 help generated by cobra |
| 18 | vault.sh:110-113 | get_file_age primary method (`find -mtime +0 \| wc -l`) yields only 0 or 1, and the mtime fallback runs only when it yields 0 — every file older than a day reports age "1"; true day counts unreachable | BUG | v2 computes age from mtime directly; listed ages become real |
| 19 | vault.sh:134-141 | format_age: ⚠ warn marker when age > 7 days, • otherwise | CONTRACT | ported; threshold becomes reachable once #18 is fixed |
| 20 | vault.sh:148 + triage_llm.py:197 | slugify strips leading markdown heading markers | CONTRACT | slugify_test.go:21 "heading" |
| 21 | vault.sh:150 + triage_llm.py:191 | forbidden filename chars replaced by space: OS set plus `@&#` | CONTRACT | slugify_test.go:23 "forbidden", "at-amp-hash" |
| 22 | vault.sh:152-154 + triage_llm.py:199 | whitespace runs collapse to one space; ends trimmed | CONTRACT | slugify_test.go:26 "collapse" |
| 23 | vault.sh:143-144 + triage_llm.py:194-203 | case preserved — titles never lowercased | CONTRACT | slugify_test.go:28 "case" |
| 24 | vault.sh:159 + triage_llm.py:196,203 | empty / whitespace-only / heading-markers-only input slugs to "Untitled" | CONTRACT | slugify_test.go:30 "only heading markers", "whitespace only" |
| 25 | vault.sh:156-158 + triage_llm.py:200-202 | over-budget titles truncate at a word boundary, no trailing space, ≤ maxLen | CONTRACT | slugify_test.go:32 "truncate 60" |
| 26 | vault.sh:157 | bash truncation `cut -c1-60` counts bytes and can split a multibyte character mid-sequence; python slices characters | BUG | v2 rune-budget Slugify + NFC normalization (slugify_test.go "nfd to nfc"; design spec §filename policy) |
| 27 | vault.sh:165-169 | is_bare_url: trim surrounding whitespace, then whole-string `^https?://` with no internal whitespace | CONTRACT | ported (phase 1 capture) |
| 28 | vault.sh:181 | fetch_url_title capped at 5s (`curl -sL --max-time 5`), UA "Mozilla/5.0 (compatible; ov-capture/1.0)" | CONTRACT | ported |
| 29 | vault.sh:187-189 | bot-challenge interstitials rejected (challenges.cloudflare.com, cf-browser-verification, cf_chl_opt, "Just a moment") instead of returning a fake title | CONTRACT | ported |
| 30 | vault.sh:175-206 | fetch_url_title never fatal: any failure (no curl/python3, empty HTML, no/empty title) prints nothing + returns non-zero; caller falls back to slugified URL | CONTRACT | ported |
| 31 | vault.sh:192-203 | title extraction: first `<title>` case-insensitive/dotall, HTML entities unescaped, whitespace collapsed; empty result = failure | CONTRACT | ported |
| 32 | vault.sh:210-218 | count_moc_items counts LINES containing `[[` (grep -c), not links — two links on one line count once; a frontmatter `moc:` line counts too | DECIDE | v1 comment admits "rough estimate"; v2 counts parsed wikilinks (phase 1 parser) |
| 33 | vault.sh:221-256 | find_moc_by_name accepts "Music" or "MOC Music"; Resources dir preferred, then first vault-wide find match | CONTRACT | ported; moc_cleanup.py find_moc_files (202-215) mirrors it |
| 34 | vault.sh:259-265 | a MOC is any file matching `MOC*.md` anywhere in the vault (also used at 892, 921, 974, 1012); listing sorted by name | CONTRACT | ported — the glob IS the MOC definition |
| 35 | vault.sh:271-278 | bash picker candidates: 4 PARA roots + every subdirectory at ANY depth, deduped sorted; python prompt uses depth ≤ 2 — two v1 depth policies coexist | DECIDE | v2: LLM prompt folders stay depth ≤ 2 (#87); human picker (phase 2) walks full depth like v1 bash |
| 36 | vault.sh:287-322,369 | folder picker prints ONLY the chosen absolute path on stdout; prompts to stderr; fzf --print-query lets the user type a brand-new path | CONTRACT | tui tty discipline (phase 2), same contract as #9 |
| 37 | vault.sh:360-367 | chosen folder that doesn't exist is created on demand (mkdir -p) after slash trimming | CONTRACT | ported (phase 2) |
| 38 | vault.sh:324-354,417-451 | non-fzf numbered-list fallbacks: out-of-range number silently treated as a literal new path; invalid MOC input recurses the picker | DECIDE | dropped: v2 ships its own TUI picker — no fzf dependency, no fallback ladder (spec §CLI/TUI) |
| 39 | vault.sh:380-385,408-411 | MOC selection always optional: no MOCs found / cancel / Enter-skip never fail the calling capture | CONTRACT | ported (phase 2) |
| 40 | vault.sh:485 | MOC entry format `- [[Title]] — snippet` | CONTRACT | ported (phase 1/2 MOC append) |
| 41 | vault.sh:487-491 | no preferred section in MOC → new "## 🔗 Recent Additions" section appended at EOF with the entry | CONTRACT | ported (part of #8's simplification) |
| 42 | vault.sh:493-507 | existing-section insert places entry immediately below the heading by rewriting the whole file through `mktemp` (defaults to $TMPDIR) + `mv` — cross-filesystem unsafe, non-atomic, no fsync | BUG | WriteNoteAtomic: same-dir dot-temp + fsync + rename (write_test.go) |
| 43 | vault.sh:521-575 | capture flags: --title/--tags/--source/--moc; `--` ends flag parsing; unknown flag → error; positionals join into the body | CONTRACT | ported (phase 1) |
| 44 | vault.sh:577-587 | positional body wins over stdin; neither → error + exit 1 (companion to #10) | CONTRACT | frozen AGENTS.md subset |
| 45 | vault.sh:590-595 | body trailing whitespace stripped; whitespace-only body refused | CONTRACT | ported |
| 46 | vault.sh:602-613 | title auto-derived from first non-empty body line; bare-URL line → fetched page title, falling back to the slugified URL | CONTRACT | ported (phase 1) |
| 47 | vault.sh:618-625 | --moc requires exact find_moc_by_name resolution; miss → error, capture aborted | CONTRACT | ported |
| 48 | vault.sh:640-653 | MOC snippet: fetched URL title or first body line; >60 chars → word-boundary truncate + "..."; bare URLs never truncated mid-path | CONTRACT | ported |
| 49 | vault.sh:655-658 | inbox filename "YYYY-MM-DD HHMM <Title>.md" | CONTRACT | frozen AGENTS.md subset |
| 50 | vault.sh:661-667 | same-minute filename collision probes " (2)", " (3)", … via `-e` until free | CONTRACT | ported (uniqueness policy per #51) |
| 51 | vault.sh:661-667 | collision check inherits filesystem case-sensitivity: on case-insensitive APFS, case-variant titles accidentally dedupe into suffixes; on case-sensitive FS they'd silently coexist and collide in Obsidian | DECIDE | superseded: v2 filename policy = NFC + case-insensitive uniqueness enforced in code, filesystem-independent (design spec §filename policy) |
| 52 | vault.sh:674-692 | capture frontmatter: type: inbox, created/modified = YYYY-MM-DD, source (default cli), optional `tags: [a, b]`, optional `moc: [[Name]]` | CONTRACT | frozen AGENTS.md subset; the moc: line is what feeds quirk #4 |
| 53 | vault.sh:694-708 | "# Title" heading always written; first body line dropped iff it equals the title modulo leading #s | CONTRACT | ported |
| 54 | vault.sh:710-716 | MOC-linked capture appends footer `*Added to [[MOC]] on <date>*` | CONTRACT | ported |
| 55 | vault.sh:719-730 | note written before MOC update; MOC update failure reported but capture already succeeded | CONTRACT | ported (same never-abort semantics as #94) |
| 56 | vault.sh:733-756 | inbox list: every inbox *.md with age marker + "(Nd old)"; empty → "✓ Inbox is empty" | CONTRACT | ported (ages become real per #18) |
| 57 | vault.sh:758-801 | manual triage keys: Enter/f = folder picker + mv, s = skip, d = delete, anything else = skip; d deletes IMMEDIATELY with no confirmation while triage --llm requires y (triage_llm.py:681-688) — the two v1 triage paths disagree | DECIDE | v2 single triage TUI; delete always confirms (align to the python path) |
| 58 | vault.sh:823 | `ov new` uses a THIRD slug rule: strip non-alphanumerics (keeping spaces) then spaces→hyphens — disagrees with slugify_title | BUG | one Slugify everywhere (internal/vault/slugify.go) |
| 59 | vault.sh:806-853 | new-note types 1-4 → template + fixed destination (Projects / Areas/Work / Areas/Learning / Inbox); empty title → error; missing template → bare "# Title"; {{title}} placeholder substituted | CONTRACT | ported (phase 2 `new`) |
| 60 | vault.sh:863-899 | review: inbox count, ≤10 files modified <7 days (Archive excluded), project listing, MOC listing, next-step hints | CONTRACT | ported (phase 2 `review`) |
| 61 | vault.sh:872,906-909 | Archive/Meta/"Daily Notes" exclusions hardcode literal "04-Archive"/"99-Meta" paths — OV_ARCHIVE/OV_META overrides ignored | BUG | v2 excludes by configured folder names |
| 62 | vault.sh:902-915 | stale: default 90 days, `stale N` overrides; excludes Archive/Meta/Daily Notes; prints vault-relative path + age | CONTRACT | ported |
| 63 | vault.sh:918-929 | mocs list description = first non-heading, non-blank line among the first 5 | CONTRACT | ported |
| 64 | vault.sh:931-968 | mocs new: "MOC <title>.md" in Resources from embedded skeleton (Overview/Key Notes/Resources/Related MOCs + created stamp); empty title → error | CONTRACT | ported (phase 2) |
| 65 | vault.sh:977 | orphan scan covers only Resources + Areas — Projects and Archive never scanned | DECIDE | keep the scope: projects are inherently linked/active, archive deliberately unlinked; recorded for the phase 1 reimplementation (#1) |
| 66 | vault.sh:1043-1048 | mocs add: entry `- [[name]]` inserted directly under "## Key Notes", or appended at EOF when that section is missing | CONTRACT | ported (phase 2) |
| 67 | vault.sh:1053-1065 | mocs cleanup dispatches to moc_cleanup.py (single name or --all); no arg → usage error | CONTRACT | ported as `ov2 mocs cleanup` (phase 3) |
| 68 | vault.sh:1082-1090,1181-1188 | publish/unpublish require OV_DOCS_HOST (error + exit 1); OV_DOCS_PATH defaults to /var/www/docs | CONTRACT | ported (config keys unified per #82) |
| 69 | vault.sh:1093-1103,1202-1220 | no file arg → gum picker (publish: vault *.md excluding .obsidian; unpublish: remote ls multi-select); hard gum dependency, error without it | DECIDE | v2 phase 2 TUI replaces gum |
| 70 | vault.sh:1115-1120 | publishing a .md without --llm refuses with a hint to use --llm | CONTRACT | ported |
| 71 | vault.sh:1123-1125 | --llm on a non-.md file warns and publishes as-is | CONTRACT | ported |
| 72 | vault.sh:1137-1145 | publish --llm pipes the note through `eval "$llm_cmd"` — shell eval of a config value (word-splitting + command injection); python side shlex-splits instead | BUG | v2 always argv-exec, never a shell (llm runner; same mechanism as #89) |
| 73 | vault.sh:1130-1133 | published slug: lowercase + spaces→hyphens + strip non-alphanumerics; output $VAULT_DIR/Published/<slug>.html | DECIDE | keep lowercase URL-style slugs for publish as a distinct documented rule (phase 4); note filenames keep case (#23) |
| 74 | vault.sh:1150-1159 | LLM output cleaned by extracting the <html>…</html> block when present, else used raw; written with trailing newline | CONTRACT | ported (phase 4) |
| 75 | vault.sh:1166-1176 | rsync -avz to host:path; prints "Live at <OV_DOCS_URL>/<file>" when the URL base is set | CONTRACT | ported (phase 4) |
| 76 | vault.sh:1191-1200 | unpublish with args: ssh `rm -f` per basename, no confirmation | CONTRACT | ported (phase 4) |
| 77 | vault.sh:1224-1234 | interactive unpublish requires explicit y/Y confirm before the ssh rm loop | CONTRACT | ported |
| 78 | vault.sh:1238,1305-1312 | bare `ov` → help, exit 0; unknown command → help + exit 1 | CONTRACT | cobra root configured to match |
| 79 | vault.sh:1246-1254,1265-1268 | `triage` = built-in manual TUI; `triage --llm` and `render` exec the python scripts with --vault | DECIDE | v2 is one binary; `--llm` stays a flag on a single `triage` command (spec §CLI/TUI) |
| 80 | vault.sh:1287-1289 | `mocs update` prints "coming soon" — dead stub | DECIDE | dropped from the v2 command surface |
| 81 | triage_llm.py:62,71-78 | legacy config line grammar: optional whitespace, OV_ key, `=`, optional quotes, value, optional trailing # comment; expandvars applied to file values | CONTRACT | ParseLegacy migrates exactly this grammar (migrate_test.go:10 TestParseLegacy) |
| 82 | triage_llm.py:39-49 vs vault.sh:25,1082-1084 | python reader recognizes only 9 keys — OV_DOCS_HOST/PATH/URL invisible to it while bash sources everything; the two v1 config readers disagree on the key inventory | BUG | fixed: v2 single 12-key inventory including docs keys (config.go, TestLoadDefaultsOnly) |
| 83 | triage_llm.py:79-85 | config precedence per key: env > file > built-in default | CONTRACT | ported (config_test.go:54 TestEnvBeatsFile) |
| 84 | triage_llm.py:108 | FM_RE closing-fence match: an inner line starting `----` gets its first 3 dashes consumed as the closing fence — frontmatter splits lossily, the 4th dash leaks into the body | DECIDE | v2 diverges deliberately: ParseNote treats it as no-frontmatter (round-trip-safe) — safer than silent corruption |
| 85 | triage_llm.py:111-136 | lenient read view: blank/# lines skipped, first ":" splits key/value, surrounding quotes stripped, `[a, b]` → list, `[]` → empty list | CONTRACT | frontmatter_test.go:36 TestLenientView; two deliberate Go divergences recorded as #121-122 |
| 86 | triage_llm.py:139-162 | render_frontmatter imposes preferred key order and re-renders values — quoting/comments/order lost even when nothing was edited | BUG | v2 no-op round-trip is byte-identical (frontmatter_test.go:6 TestRoundTripByteIdentical; review blocker F6/m5) |
| 87 | triage_llm.py:169-184 | folder discovery: each PARA root + subdirs to depth 2, sorted, missing roots skipped, plain files ignored | CONTRACT | discover_test.go:10 TestDiscoverFolders |
| 88 | triage_llm.py:177-182 | Path.is_dir() follows symlinks — a symlinked subdirectory is listed and recursed; Go's DirEntry.IsDir() does not at sub-levels | DECIDE | resolved (scope: subdirectories only): symlinked PARA *roots* ARE followed — DiscoverFolders checks roots with os.Stat, which resolves symlinks; only symlinked *subdirectories* are excluded (DirEntry.IsDir does not follow). Avoids walk cycles and vault-escaping triage targets below a root. Divergence accepted. |
| 89 | triage_llm.py:254-258 | OV_LLM_CMD shlex-split into argv — never a shell; --model appended when set; prompt delivered on stdin | CONTRACT | ported (phase 3 llm runner); also the model for fixing #72 |
| 90 | triage_llm.py:259-275 | LLM failure modes: missing binary → clean error; timeout 120s (180s for cleanup, moc_cleanup.py:272); non-zero exit → error including stderr; stdout returned only on success | CONTRACT | ported |
| 91 | triage_llm.py:214-241 | proposal schema fields: to / from / new_title / frontmatter_patch / links_to_add / body_patch / confidence / rationale; v1 pins links_to_add=[] and body_patch=null; "to" restricted to listed existing folders; created/modified excluded from patch; confidence ∈ high/medium/low | CONTRACT | phase 3 prompt builder keeps the schema; v2 Validate enforces the pins (#5) |
| 92 | triage_llm.py:281-304 | extract_json 3-tier fallback: direct parse → fenced ``` block → first-{ to last-}; failures raise with the raw response attached | CONTRACT | decode_test.go:9 TestExtractJSON |
| 93 | triage_llm.py:100-101,159-162,311-335,702-707 | color/_fm_line/show_note/show_proposal/summary: display-only helpers (body preview capped at 1200 chars) | no observable contract | v2 TUI restyles freely |
| 94 | triage_llm.py:376-405 | rename MOC-sync is best-effort: runs only when the title changed; missing MOC or entry → explicit ⚠ check-manually warnings; any exception swallowed — never aborts the completed move | CONTRACT | ported (phase 3) |
| 95 | triage_llm.py:366-373 | _resolve_moc_path prepends "MOC " unless the name already starts with it (case-insensitive); exact filename match via vault-wide rglob | CONTRACT | ported with #33 |
| 96 | triage_llm.py:408-437 | update_moc_entry_title replaces every [[old]] with [[new]] in the MOC body only — the frontmatter block is never touched; returns False on no-op or no match | CONTRACT | ported (tests/test_triage_llm.py:45-104) |
| 97 | triage_llm.py:458-460 | target's first path component must be a PARA root or the inbox | CONTRACT | v2 Validate (phase 3) |
| 98 | triage_llm.py:462-468 | directory target (or trailing "/") gets its filename from the slugified new_title (source stem as fallback); missing extension → ".md" appended | CONTRACT | ported |
| 99 | triage_llm.py:471-474 | existing target file → loud refusal, never overwrite; parent dirs created on demand | CONTRACT | ported; enforced mechanically by WriteNoteAtomic ErrExists (write_test.go TestCreateNewRefusesExisting) |
| 100 | triage_llm.py:477-487 | frontmatter merge on file: `type: inbox` dropped; patch applied except created/modified (script-owned); created preserved-or-today; modified always today | CONTRACT | ported (phase 3) |
| 101 | triage_llm.py:494-495 | apply = plain write_text then unlink source — no temp file, no fsync; a crash leaves a partial target beside the intact source (or a duplicate note) | BUG | v2 WriteNoteAtomic + ordered move (phase 3; write_test.go) |
| 102 | triage_llm.py:506-517,659-696 | interactive key map a/e/s/d/r/q: first char matched, Enter = skip, EOF = quit; delete requires y confirm; q prints summary and exits 0 | CONTRACT | v2 triage TUI keeps the key map (phase 3) |
| 103 | triage_llm.py:520-557 | edit_proposal: field-by-field prompts; destination accepted as folder-list index or literal path | DECIDE | phase 3 TUI redesign supersedes the exact prompts; index-or-path affordance kept |
| 104 | triage_llm.py:564-613 | --vault beats config; AGENTS.md must exist at vault root (exit 2); inbox dir must exist (exit 2); notes processed in sorted name order; --limit N caps; --dry-run shows proposals and moves nothing | CONTRACT | ported (phase 3) |
| 105 | triage_llm.py:710-716 + moc_cleanup.py:325-331 | Ctrl-C → "interrupted", exit 130 | CONTRACT | ported |
| 106 | internal/vault/write.go (no v1 source — v1 had no concurrent-edit guard anywhere, see #42, #101) | conditional write: stale content hash → visible refusal, never clobber | CONTRACT | new in v2 (review F1); write_test.go:48 TestConditionalRefusesStale |
| 107 | moc_cleanup.py:50-66 | a wikilink sharing its line with a URL is "anchored" — its display title may be retitled freely; a bare wikilink (no URL on the line) is immutable and must survive verbatim | CONTRACT | phase 3 validator (tests/test_moc_cleanup.py:94-129) |
| 108 | moc_cleanup.py:80-85 | any change to the frontmatter block → whole proposal rejected | CONTRACT | phase 3 (tests/test_moc_cleanup.py:79-91) |
| 109 | moc_cleanup.py:87-104 | dropped bare wikilinks or dropped URLs → reject the ENTIRE proposal; ValidationError semantics = never partially apply | CONTRACT | phase 3 (tests/test_moc_cleanup.py:48-76,132-146) |
| 110 | moc_cleanup.py:107-154 | cleanup prompt contract: allowed = regroup under ###, retitle garbled entries, fix formatting, reorder; forbidden = delete/invent/frontmatter/prose rewrites/## heading text; duplicates flagged, never merged; JSON shape new_content / duplicates_flagged / summary | CONTRACT | phase 3 prompt builder (tests/test_moc_cleanup.py:19-34) |
| 111 | moc_cleanup.py:161-185 | unified diff colorized git-style (+green/−red); identical input → "(no changes proposed)" | CONTRACT | phase 3 diff view (tests/test_moc_cleanup.py:152-163) |
| 112 | moc_cleanup.py:188-199 | response must contain string field new_content, else error carrying the raw payload; duplicates_flagged/summary defaulted | CONTRACT | phase 3 (tests/test_moc_cleanup.py:169-188) |
| 113 | moc_cleanup.py:202-215 | find_moc_files: --all → every MOC*.md sorted; name matches "Music" or "MOC Music"; miss → empty and exit 2 upstream | CONTRACT | ported with #33 |
| 114 | moc_cleanup.py:229-234 | MOC name XOR --all enforced, else exit 2 | CONTRACT | ported |
| 115 | moc_cleanup.py:288-291 | proposal identical to original → "already well-organized", nothing written | CONTRACT | ported |
| 116 | moc_cleanup.py:303-315 | apply gate: explicit "y" only (default N; EOF = n); the file is written only after validation AND confirmation; write is plain write_text | CONTRACT | ported; v2 writes via WriteNoteAtomic (fixes the non-atomic write, same family as #101) |
| 117 | render_html.py:258,266-279 | HTML↔MD pairing via `<!-- RENDER_SOURCE: rel/path.md -->` comment (path relative to vault root); HTML without the marker is invisible to render | CONTRACT | phase 4 render |
| 118 | render_html.py:291-304 | regeneration splices ONLY between RENDER_BODY_START/END markers; everything outside is preserved; missing markers → skip with warning | CONTRACT | phase 4 |
| 119 | render_html.py:298,306-314 | RENDER_TIMESTAMP comment updated in place, or inserted immediately after RENDER_SOURCE on first render | CONTRACT | phase 4 |
| 120 | render_html.py:301-304 | body injected via re.sub with generated HTML as the replacement TEMPLATE — literal `\1`/`\g` sequences in note content are interpreted as group references (corruption or exception) | BUG | v2 splices by index/string concatenation, never a regex replacement template |
| 121 | triage_llm.py:120-126 | lenient view parses ANY line containing ":" as a key — an indented continuation line like `  owner: alex` under a nested map becomes the top-level key `owner` | DECIDE(v2 safer) | v2 keyLine requires zero indent: indented `key: value` continuation lines are opaque — never read, patched, or deleted as keys |
| 122 | triage_llm.py:128-134 | quotes stripped BEFORE list detection: `tags: "[a, b]"` loses its quotes and then parses as the list [a, b] | DECIDE(v2 safer) | v2 detects the list shape first, strips quotes after: a quoted `"[a, b]"` stays the string the author wrote |
| 123 | design spec §CLI/TUI; phase 1 | v1 read commands print colored, decorated output entirely to stdout | DECIDE | v2 read-only commands split streams: machine-readable records → stdout (plain, no ANSI); chrome (headers, counts, empty-state, hints) → stderr; pipe-safe. No ANSI color/emoji on stdout in phase 1 |
| 124 | vault.sh:867 vs 742 | review inbox COUNT uses recursive `find` while `inbox` list uses a top-level glob — the two counts can disagree | DECIDE | unified: review inbox count = `vault.ListInbox` (top-level *.md), same source as `ov2 inbox` |
| 125 | vault.sh:872-875 | review "modified this week" takes `head -10` of unstable `find` order | DECIDE | v2 `ModifiedWithin` sorts ModTime desc, then Name asc; the command caps at 10 — deterministic |
| 126 | vault.sh:993 | orphan match was a case-sensitive substring `grep` | DECIDE | v2 keys wikilink targets and note names by NFC + case-folded basename (Obsidian-style resolution); backs the row #1/#2 reimplementation |
| 127 | vault.sh:880-887 | review lists Projects children with 📁 (dir) / 📄 (file) glyphs | DECIDE | glyph distinction is decoration; v2 machine record emits `project\t<name>` for dirs and files alike |
| 128 | vault.sh:717 | capture writes the new note via a plain `>` shell redirect — no temp file, no fsync, non-atomic (same defect family as #42/#101) | BUG | v2 capture writes via WriteNoteAtomic create-new mode (phase 2) |
| 129 | vault.sh:489-491,493-507 | MOC entry update writes are non-atomic on BOTH branches: new-section append is `>>` with no fsync, existing-section insert is mktemp (`$TMPDIR`, cross-filesystem unsafe per #42) + `mv` | BUG | v2 MOC entry append goes through vault.AppendMOCEntry (pure text transform) + WriteNoteAtomic conditional write (phase 2) |
| 130 | triage_llm.py:456 (row #6) + vault.sh:791 `mv "$file" "$dest_dir/"` | v1 bash manual triage's `mv` has no containment check at all — a typed destination (or a symlinked PARA subdir) could resolve outside the vault | BUG | v2 vault.ContainPath: EvalSymlinks both sides + filepath.Rel + reject absolute/..-prefixed results, applied to every triage move target including typed new-folder paths (phase 2 manual triage, phase 3 LLM triage reuses it) |
| 131 | design spec §Safety item 4 | no v1 equivalent — v1's fetch_url_title has no SSRF defense beyond the 5s timeout | DECIDE(new in v2) | TitleFetcher performs a post-DNS IP check on every resolved address, refusing loopback/private/link-local/CGNAT (100.64.0.0/10) targets before connecting; dials the checked IP directly (not the hostname) to close the DNS-rebinding TOCTOU window (phase 2) |
| 132 | design spec §CLI/TUI, row #57 | manual triage delete: v1 bash deletes immediately with no confirmation, disagreeing with the LLM triage path's y-confirm (triage_llm.py:681-688, row #57 DECIDE) | DECIDE | v2 single triage TUI requires an explicit huh.Confirm before delete, aligning both paths (phase 2) |
| 133 | design spec §Web layer | no v1 equivalent — v1 has no web server | DECIDE(new in v2) | `ov2 serve` binds 127.0.0.1 by default; refuses non-loopback binds unless the address is on a Tailscale interface (100.64.0.0/10) or an explicit long-named override flag is set (phase 2) |
| 134 | design spec §Web layer | no v1 equivalent | DECIDE(new in v2) | localhost hygiene middleware: Host header validated against the configured bind; state-changing routes (POST) require same-origin/absent Origin AND the `HX-Request` header (phase 2) |
| 135 | design spec §Web layer | no v1 equivalent | DECIDE(new in v2) | web capture form's title-fetch is an explicit opt-in checkbox — the server never auto-fetches a URL from a web request (distinct from CLI capture's automatic bare-URL fetch, row #46, since a web request's URL is more directly attacker-reachable) (phase 2) |
| 136 | design spec §CLI/TUI tty discipline, rows #9/#36 | no v1 equivalent for capture specifically — v1 bash always shows a picker when --moc is omitted, blocking on read even under cron/agent invocation | BUG | v2 capture's MOC picker only launches when stdin AND stdout are both an interactive tty; otherwise MOC selection is silently skipped (never blocks), matching row #39's non-fail semantics (phase 2) |
| 137 | vault.sh:661-667 | v1 collision probe is unbounded (`while [ -e ... ]; do n=$((n+1)); done`) — a pathological case never terminates | DECIDE(v2 safer) | v2 caps the collision suffix probe at a documented bound and returns an error rather than looping forever (phase 2) |
| 138 | design spec §Web layer | no v1 equivalent | DECIDE(new in v2) | web inbox list (`GET /`) renders through the same `vault.ListInbox` query phase 1 already exposes via CLI — one source of truth, no separate web-only inbox query (phase 2) |
| 139 | vault.sh:791 `mv "$file" "$dest_dir/"` | plain mv silently overwrites an existing same-name file in the destination folder | BUG | v2 triage move refuses when the destination already exists (mirrors row #99's existing-target refusal), surfacing a visible error instead of a silent overwrite (phase 2) |

## Cross-check: phase 0 test tags → rows

Every `// CONTRACT:` / `// BUG(fixed):` / `// DECIDE:` comment in
`internal/**/*_test.go` (Tasks 2–8; Task 9's doctor tests carry no tags):

| Test comment | Row |
|---|---|
| config_test.go:54 CONTRACT env wins over file | #83 |
| config_test.go:68 DECIDE(one rule) VaultDir expansion | #15 |
| config_test.go:88 CONTRACT $OV_CONFIG overrides path | #11 |
| migrate_test.go:10 CONTRACT legacy config line regex | #81 |
| decode_test.go:9 CONTRACT 3-tier JSON fallback | #92 |
| discover_test.go:10 CONTRACT depth ≤ 2, sorted | #87 |
| frontmatter_test.go:6 CONTRACT byte-identical round-trip | #86 |
| frontmatter_test.go:36 CONTRACT lenient read view | #85 |
| frontmatter_test.go:54 CONTRACT(by accident) wikilink quirk | #4 |
| slugify_test.go:10 DECIDE(kept) 60/80 budget split | #3 |
| slugify_test.go:21 CONTRACT heading markers stripped | #20 |
| slugify_test.go:23 CONTRACT forbidden set includes @&# | #21 |
| slugify_test.go:26 CONTRACT whitespace collapse + trim | #22 |
| slugify_test.go:28 CONTRACT case preserved | #23 |
| slugify_test.go:30 CONTRACT heading strip may leave nothing | #24 |
| slugify_test.go:32 CONTRACT word-boundary truncation | #25 |
| write_test.go:48 CONTRACT(new in v2, review F1) stale hash refusal | #106 |

## Coverage: v1 function → rows

**vault.sh** — load_config #11-16 · show_help #17 · get_file_age #18 ·
format_age #19 · slugify_title #3,20-26 · is_bare_url #27 · fetch_url_title
#28-31 · count_moc_items #32 · find_moc_by_name #33 · list_all_mocs #34 ·
list_all_para_folders #35 · select_target_folder_interactive #36-38 ·
select_moc_interactive #9,38,39 · update_moc #8,40-42 · capture_note
#10,43-55 · inbox_list #56 · triage_inbox #57 · new_note #7,58,59 ·
review_vault #60,61 · find_stale #61,62 · moc_list #63 · moc_new #7,64 ·
moc_orphan #1,2,65 · moc_add #66 · moc_cleanup #67 · publish_doc #68-75 ·
unpublish_doc #68,69,76,77 · render dispatch #79,117-120 · main dispatch
#78-80

**triage_llm.py** — load_config #11,81-83 · color #93 · split_frontmatter
#4,84,85,121,122 · render_frontmatter #86 · _fm_line #93 · discover_folders #87,88 ·
slugify_title #3,20-25 · call_llm #89,90 · extract_json #92 · show_note #93 ·
show_proposal #93 (and the #5 display gap) · extract_moc_name_from_frontmatter
#4 · _resolve_moc_path #95 · sync_moc_link_after_rename #94 ·
update_moc_entry_title #96 · apply_proposal #5,6,97-101 · prompt_choice #102 ·
edit_proposal #103 · main #102,104,105 · summary #93

**moc_cleanup.py** — _frontmatter_block #108 · _bare_wikilinks #107 ·
validate_proposal #107-109 · build_prompt #110 · render_diff #111 ·
parse_llm_response #112 · find_moc_files #113 · main #105,114-116

**render_html.py (RENDER_SOURCE splicing)** — find_paired_files #117 ·
regenerate #118-120
